api_version: "mender/v1"
kind: "manifest"
name: "system-core-v3"
system_types_compatible: ["system-core"]


component_types:
  gateway:
    artifact_name: gateway-v2
    update_strategy:
      order: 10

  rtos:
    artifact_name: rtos-v3
    update_strategy:
      order: 20
